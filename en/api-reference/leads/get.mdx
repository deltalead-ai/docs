---
title: "Fetch One Lead by ID — DeltaLead REST API Endpoint"
sidebarTitle: "Get Lead"
description: "Retrieve all data for one lead by ID — contact, status, score, channels, vehicles of interest, notes, AI Summary, metadata, and timestamps."
---

<Note>
  The REST API is in early access and is enabled per account on request. API keys are not yet issued from the dashboard. To request access, contact your account executive.
</Note>

Fetch the full detail of a lead by its identifier. Use this endpoint to display a lead inside your own tooling, sync a record to an external CRM, or read the latest state before taking an action.

## Endpoint

```text
GET https://platform-api.deltalead.ai/v1/leads/{id}
```

## Path Parameters

<ParamField path="id" type="string" required>
  The unique identifier of the lead. Lead IDs are prefixed with `lead_`, e.g. `lead_abc123`. You can obtain them from [List Leads](/en/api-reference/leads/list) or from webhook payloads.
</ParamField>

## Example Request

```bash cURL
curl https://platform-api.deltalead.ai/v1/leads/lead_abc123 \
  -H "Authorization: Bearer dl_live_YOUR_KEY"
```

## Response

A successful request returns HTTP `200 OK` with the full lead object. It is the same shape each element of [List Leads](/en/api-reference/leads/list) returns.

```json 200 OK
{
  "id": "lead_abc123",
  "organization_id": "org_00112233",
  "status": "qualified",
  "source": "landing-page",
  "score": 87,
  "contact": {
    "first_name": "María",
    "last_name": "García",
    "email": "maria@example.com",
    "phone": "+5491155551234",
    "display_name": "María García"
  },
  "channels": ["whatsapp", "custom"],
  "interested_vehicles": ["veh_9f2c41"],
  "tags": ["financiacion"],
  "assigned_to": "user_xyz789",
  "notes": [
    {
      "id": "note_4d7e21",
      "body": "Consultó por financiación a 36 cuotas.",
      "author_id": "user_xyz789",
      "source": "manual",
      "created_at": "2024-03-10T15:02:11Z",
      "updated_at": "2024-03-10T15:02:11Z"
    }
  ],
  "ai_summary": "Consulta por una Hilux SRX 2024 con financiación a 36 cuotas. Pidió que la contacten por la tarde.",
  "ai_summary_status": "completed",
  "metadata": {
    "crm_id": "CRM-88213",
    "utm_campaign": "hilux-marzo"
  },
  "last_activity_at": "2024-03-11T09:05:33Z",
  "created_at": "2024-03-10T14:22:00Z",
  "updated_at": "2024-03-11T09:05:33Z",
  "expire_at": null
}
```

### Response Fields

<ResponseField name="id" type="string">
  Unique identifier for the lead, prefixed with `lead_`.
</ResponseField>

<ResponseField name="organization_id" type="string">
  Identifier of the organization (dealership) that owns the lead.
</ResponseField>

<ResponseField name="status" type="string">
  Current status within the pipeline. One of `new`, `contacted`, `qualified`, `appointment`, `proposal`, `incubating`, `won`, `lost`.
</ResponseField>

<ResponseField name="source" type="string">
  Origin label reported by the system that generated the lead, e.g. `landing-page`. It is free text, always present, and does not replace `channels`.
</ResponseField>

<ResponseField name="score" type="integer">
  Qualification score assigned by DeltaLead. An unqualified lead scores `0`.
</ResponseField>

<ResponseField name="contact" type="object | null">
  Contact details for the person. Name, email, and phone live here, never at the root of the lead. `null` on a lead that has no contact details associated yet.
</ResponseField>

<ResponseField name="contact.first_name" type="string | null">
  Given name.
</ResponseField>

<ResponseField name="contact.last_name" type="string | null">
  Family name.
</ResponseField>

<ResponseField name="contact.email" type="string | null">
  Email address. `null` if unknown.
</ResponseField>

<ResponseField name="contact.phone" type="string | null">
  Phone number, normally in E.164 format. `null` if unknown.
</ResponseField>

<ResponseField name="contact.display_name" type="string">
  Display name, composed from `first_name` and `last_name` when none was provided.
</ResponseField>

<ResponseField name="channels" type="array of string">
  Channels the lead has communicated through. It is an array because a lead accumulates them. Possible values: `typeform`, `mercadolibre`, `whatsapp`, `custom`, `manual`, `odoo`.
</ResponseField>

<ResponseField name="interested_vehicles" type="array of string">
  IDs of the catalogue vehicles the lead asked about. They are identifiers only — resolve them against the catalogue for the vehicle data. The shape is identical in [List Leads](/en/api-reference/leads/list).
</ResponseField>

<ResponseField name="tags" type="array of string">
  Tags applied to the lead by your team.
</ResponseField>

<ResponseField name="assigned_to" type="string | null">
  ID of the salesperson the lead is assigned to. `null` if unassigned.
</ResponseField>

<ResponseField name="notes" type="array of object">
  Notes on the lead, oldest first. Always an array of objects.
</ResponseField>

<ResponseField name="notes[].id" type="string">
  Identifier of the note.
</ResponseField>

<ResponseField name="notes[].body" type="string">
  Note text.
</ResponseField>

<ResponseField name="notes[].author_id" type="string">
  ID of the person or process that wrote the note.
</ResponseField>

<ResponseField name="notes[].source" type="string">
  Origin of the note. One of `manual`, `typeform`, `legacy`, `ai_agent`, `custom_webhook`.
</ResponseField>

<ResponseField name="notes[].created_at" type="string">
  ISO 8601 timestamp of when the note was created.
</ResponseField>

<ResponseField name="notes[].updated_at" type="string | null">
  ISO 8601 timestamp of the last edit to the note. `null` if the note was never edited.
</ResponseField>

<ResponseField name="ai_summary" type="string | null">
  AI Summary of the conversation. `null` while `ai_summary_status` is anything other than `completed`.
</ResponseField>

<ResponseField name="ai_summary_status" type="string">
  State of the AI Summary. One of `not_requested`, `pending`, `completed`, `failed`.
</ResponseField>

<ResponseField name="metadata" type="object">
  The `metadata` object your integration sent to the [Custom Webhook](/en/api-reference/webhooks/custom-inbound), promoted to the root of the lead and returned verbatim. This is the field that lets you confirm your own payload made it through intact, and re-associate the lead with the record in your source system. `{}` on a lead that arrived without metadata.
</ResponseField>

<ResponseField name="last_activity_at" type="string | null">
  ISO 8601 timestamp of the last interaction recorded for this lead. `null` on a lead that has recorded none yet. This is the field `last_activity_after` and `last_activity_before` filter on.
</ResponseField>

<ResponseField name="created_at" type="string">
  ISO 8601 timestamp of when the lead was created.
</ResponseField>

<ResponseField name="updated_at" type="string">
  ISO 8601 timestamp of the most recent change to any field on the lead.
</ResponseField>

<ResponseField name="expire_at" type="string | null">
  ISO 8601 timestamp at which the lead deletes itself. Always `null` on a live lead — live leads never expire. On a lead created with a `dl_sandbox_` key it carries the automatic purge date, so a sandbox lead that disappears is honouring the documented contract, not going missing.
</ResponseField>

## Errors

<Accordion title="404 — Lead not found">
  Returned when no lead with that `id` exists in your organization.

  ```json 404 Not Found
  {
    "status": "error",
    "detail": "lead_not_found"
  }
  ```

  An ID belonging to another organization also returns `404`. Branch on the `detail` identifier, never on the text — see [Errors](/en/api-reference/errors).
</Accordion>